version: '3'

services:
    # mongo:
    #     image: mongo
    #     ports:
    #         - 27017:27018
    #     volumes:
    #         - mongodb:/data/db
    hotel_manage_server:
        restart: always
        build:
            context: .
            dockerfile: Dockerfile
        image: blackparadise0407/hotel_manage_server
        env_file:
            - .prod.env
        environment:
            - NODE_ENV=production
        ports:
            - '8008:8008'
# volumes:
# mongodb:
